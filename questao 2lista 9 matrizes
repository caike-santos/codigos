// Online C compiler to run C program online
#include <stdio.h>


int trocar(int lin, int col, int m[lin][col], int c1, int c2){
    int i, j, k, p;
    if(c1>c2){
        return 0;
    }else{
    for(i=0; i<lin; i++){
        p = c2;
        for(j=c1; j<=c2/2; j++){
            k = m[i][j];
            m[i][j] = m[i][p];
            m[i][p] = k;
            p--;
        }
    }
} return 1;}
void exibirMatriz(int x, int y, int m[x][y])
{
    int i, j;

    for(i=0;i<x;i++)
    {
        for(j=0;j<y;j++)
        {
            printf("%d ", m[i][j]);
        }
        printf("\n");
    }
}

int main() {
    int m[9][7] = {
        {1, 2, 4, 8, 6, 2, 0},
        {9, 7, 4, 0, 3, 1, 0},
        {8, 8, 4, 1, 2, 0, 1},
        {4, 7, 2, 1, 5, 4, 8},
        {2, 1, 2, 4, 0, 1, 0},
        {1, 8, 7, 2, 1, 3, 6},
        {9, 4, 5, 2, 0, 3, 2},
        {0, 1, 3, 5, 6, 9, 8},
        {7, 4, 1, 2, 3, 5, 0}
    };
    int c1 = 0, c2 = 4;
    if(trocar(9, 7, m, c1, c2) == 1){
    exibirMatriz(9, 7, m);}
    else{
        printf("valor invalido");
    }
    return 0;
}
